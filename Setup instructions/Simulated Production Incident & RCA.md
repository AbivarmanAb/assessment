Use a GitHub repo with a buggy app.

Run the app locally.

Capture logs (docker logs <container> or kubectl logs <pod>).

Reproduce the bug.

Document RCA:

Root cause

Impact

Fix (code or config change)

Prevention (unit tests, monitoring)
